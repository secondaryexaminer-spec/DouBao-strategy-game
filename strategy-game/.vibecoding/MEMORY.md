# 项目记忆 · 边境指挥官 / Frontier Commander

> 跨对话记忆。只记录"值得后续会话知道"的内容：踩坑、决策、偏好、根因链。纯执行细节不写这里。

## 项目速览（2026-09-05 建档）
- 纯前端回合制策略游戏 v0.1.2；`src/main.js`（约 161KB，重构中）含逻辑；esbuild 打包为 `js/game.js`（IIFE，file:// 可开）。
- 单位类型（60 种）：陆军 10（民兵/侦察/长枪/剑士/弓箭/弩手/工程师/骑兵/近卫/投石车）+ 海军 5（桨帆船/战船/战舰/驳船/运兵船）+ 神罗专属 6（重甲步兵/长矛方阵/帝国弩手/帝国近卫军/攻城塔/重型投石车）+ 神罗国家特色 3（奥地利骑士/普鲁士掷弹兵/巴伐利亚山地弩手）+ 金帐专属 6（轻骑兵/汗国骑兵/骑射手/游牧弓手/快速桨帆船/游牧战车）+ 金帐国家特色 3（可汗亲卫/骆驼骑兵/游牧重炮）+ 威尼斯专属 6（海军陆战队/桨帆战舰/大师工程师/威尼斯战舰/商队/雇佣剑士）+ 威尼斯国家特色 3（威尼斯巨舰/热那亚海军弩手/拉古萨巨商队）+ 马穆鲁克专属 6（马穆鲁克骑兵/圣战者/阿拉伯弓手/骆驼骑兵/阿拉伯帆船/攻城弩）+ 马穆鲁克国家特色 3（苏丹禁卫军/叙利亚长弓手/哈里发学者）+ 大明专属 6（神机营火枪兵/戚家军/大明骑兵/工部工程师/宝船/红夷大炮）+ 大明国家特色 3（锦衣卫/朝鲜龟船/安南象兵）。远程 4：投石车、战舰。
- **联盟系统（完成）**：5 联盟（神圣罗马帝国/金帐汗国/威尼斯共和国/马穆鲁克苏丹国/大明帝国），15 国家（每联盟 3 国）。constants.js 有 FACTIONS/NATIONS 定义；开始界面玩家和每个 AI 都有联盟/国家两级下拉框（联盟切换联动国家列表）；game.settings 含 faction/nation，aiProfiles 含 faction/nation；buildableTypes 已按联盟+国家过滤（通用兵种全联盟可用，联盟专属仅该联盟，国家特色仅该国家）；开局日志显示联盟/国家/特色兵种。
- **创新机制（全部完成：10联盟机制+28国家机制+7兵种效果）**：
  - 联盟机制：征召兵(神罗)/圣战(马穆鲁克)/帝国议会(神罗)/打完就跑(金帐)/火器齐射(大明)/卫所制(大明)/商路经济(威尼斯)/雇佣兵(威尼斯)/马穆鲁克精锐/光环单位(巴格达)
  - 国家机制：奥地利(城市收入+2/骑兵charge+1)、普鲁士(全单位移动+1/据点伤害+3)、巴伐利亚(丘陵不消耗/回血+1)、金帐本部(骑兵攻击+1/营地+2回合)、白帐(沙漠不消耗/对骑兵+2)、蓝帐(步兵防御+1/雪地不消耗)、威尼斯本部(海军攻击+1/造船-20%)、热那亚(收入+10%/海军远程+1)、拉古萨(生产-5%/商队占领收入+5)、埃及(城市收入+2/骑兵击杀回血3)、叙利亚(远程+1/对异联盟+1)、巴格达(光环3格/光环单位防御+2)、大明本部(造船-10%/溅射60%)、朝鲜(丘陵不消耗/海军反弹30%)、安南(森林不消耗/对步兵+5)
  - 兵种效果：长矛方阵(免冲锋)/帝国近卫军(守点+3)/攻城塔(占领返还移动)/锦衣卫(免反击)/苏丹禁卫(击杀回血3)/朝鲜龟船(反弹30%)/安南象兵(对步兵+5)
- **雇佣兵/造船费用实现细节**：`factionAdjustedCost(owner, type, cargoTypes)` 辅助函数统一处理雇佣兵溢价(威尼斯+50%)、拉古萨折扣(-5%)、威尼斯本部造船(-20%)、大明本部造船(-10%)。注意不要和已有的 `unitBuildCost(unitEntry)` 出售退款函数重名。
- **据点命名与改名**：初始据点用全局历史名字列表；非中立据点被敌对方占领后40%概率按占领者国家命名体系改名（15国×约42个名字=约630个名字，覆盖城市/港口/堡垒/油田/军营）。
- 地形（9 种）：草原/森林/山脉(不可通行)/道路/海域/**沙漠(cost2)**/**沙地(海岸)**/**丘陵(cost2,def2)**/**雪地(cost2,def1,寒带)**。mapgen.js 自动生成：丘陵/沙漠随机撒点、雪地在地图顶部15%、沙地在水域相邻。
- 冲锋（charge）：骑兵被动属性，触发条件=本回合未移动+相邻攻击+非反击，效果=charge值加到伤害。不是主动技能。
- **AI 阵营分配**：每个 AI 可在开始界面选阵营/国家（与玩家相同的两级下拉框，阵营切换联动国家）。aiProfiles 含 faction/nation 字段。buildableTypes 已按阵营过滤，AI 只能造己方阵营兵种+通用兵种。
- 无头模拟：`sim/harness.js`（DOM shim）+ `sim/run.js`（批量对局）+ `sim/suite.js`（4 场景平衡回归）。AI 逻辑与浏览器共用同一打包产物，零分叉。
- git：仓库在 `E:\WorkPlace\DouBao-WorkPlace\strategy-game`（外层），远程已改为 `https://github.com/secondaryexaminer-spec/DouBao-strategy-game.git`。
- 重构进度：第一阶段抽取中——已抽 `src/core/mapgen.js`（地图生成）、`src/core/teams.js`（阵营判定）、`src/core/combat.js`（战斗计算）、`src/core/rng.js`（确定性 LCG）、`src/core/movement.js`（寻路 reachable/passable）；每次抽取均用同种子模拟验证行为等价。main.js 现约 157KB。game 对象已加 w/h 字段供 core 模块使用。
- 待抽：`turn.js`（回合/胜负）、`economy.js`（收入/生产）、`factory.js`（单位/据点工厂）。重构为长期贯穿性工作，随新功能需求驱动。

## 已知问题清单（待处理）
1. ~~存档路径提示过期~~：已修复（2026-09-06）
2. **`src/sim/` 空目录**：无内容，疑似遗留，可清理或忽略。
3. **平衡性问题（待办）**：新增 4 兵种后 suite 需重新跑基线。旧有问题：mirror-strait B 胜率偏低、diff-gap 冷酷优势不足。后续 AI 改良+兵种调参时统一处理。（2026-09-06 复测：加威尼斯 9 兵种后 sim:suite 3/4 通过，diff-gap 冷酷 B 胜率仍 50%，与 d2be63d 基线完全一致——确认非威尼斯兵种引入的回归）
4. **阶段4 攻城/守点已补但 AI 无针对性使用**：投石车/重型投石车/攻城弩/游牧战车/游牧重炮已获对据点驻军伤害（SIEGE_DAMAGE），近卫军守点+1，但 AI buildScore 无"攻城优先"策略（阶段6 与 AI 改良一起处理）。
5. **§15 职责重复 3 组（阶段5 平衡处理）**：imperialCrossbow vs crossbow（+1 atk+2 cost）、hordeCavalry vs cavalry（同 charge2/atk8 便宜 6）、mingCavalry vs cavalry（同 charge2/atk8 便宜 2）——联盟专属=通用微调，缺玩法差异，阶段5 给差异化（机制或数值）。
4. **AI 对新兵种无针对性策略**：buildScore 对桨帆船/驳船/战舰/投石车走通用评分，没有特殊规则（如远程单位走位、战舰护航）。后续 AI 改良时优化。
5. **雇佣兵老规矩（factionAdjustedCost 威尼斯 1.5×）待处理**（2026-09-07 用户拍板：**先不删**，阶段5 平衡时处理，**务必别忘**）：现状=威尼斯普通界面造任何非己方兵种 ×1.5（老规矩）与威尼斯对话做的"雇佣兵市场"（3 种兵 ×1.5）**双入口并存**。规格书 §7.4 本意=用市场取代随便造兵，需配套"收窄威尼斯普通界面可造兵种"（完全禁止/只留本国兵），只删 1.5× 会让威尼斯原价万能造兵且市场白做。注意 factionAdjustedCost 是四合一函数（威尼斯 1.5×/拉古萨 0.95×/威尼斯本部造船 0.8×/大明本部造船 0.9×），被 4 处调用（buildAtSite 1499/transport 1581,1601/AI 生产 2844），删必须精确到威尼斯那一个条件，勿误伤其余三条。

## 路线图（用户确认）
1. **地基重构**（进行中）：core 层拆分（mapgen ✅ / teams ✅ / combat ✅ / rng → movement → turn）
2. **模拟器升级**：直接 import core/ai 模块（替代 eval 打包产物）+ 确定性 RNG + 状态快照 trace
3. **同步开始**（地基+模拟器差不多后）：增加新兵种 / 改良 AI 算法让 AI 更有策略思维（以冷酷难度为训练基准）

## v0.2 差异化重构（2026-09-06 启动，最高优先级主线）
- **依据**：项目根 `边境指挥官 v0.2——五大联盟与十五国家差异化重构及实现规格书.md`（62章）+ `v0.2-改造施工图.md`（执行层，改前必读）。
- **总目标**：从"属性差异"升级为"玩法差异"——每联盟一个 Layer4 核心系统：神罗=工事、金帐=掠袭+游牧营地、威尼斯=贸易网络、马穆鲁克=精锐成长(veterancy)、大明=工程+火力网。机制分4层：L1属性/L2条件/L3行为/L4系统，L4才是联盟身份核心。
- **实施顺序（严格）**：阶段0审计✅ → **阶段1基础设施（主对话，进行前置）**：core/events.js(EventBus)+status.js(中央时效状态)+structures.js(战场设施)+factions/factionContext.js(依赖注入)+factionRegistry.js；game新增 factionState/statuses/structures 三字段（旧存档容错）；main.js 只在7埋点(beginTurn/advanceTurn/attack前后/removeUnit击杀/captureSite/grantIncome)emit。→ 阶段2五大L4系统（**各开一个附属对话**，顺序A神罗→B金帐→C威尼斯→D马穆鲁克→E大明，前一个集成验收再开下一个）→ 阶段3十五国机制重做 → 阶段4兵种审查+tags → 阶段5数值平衡(最后) → 阶段6 AI对局测试(此时才练AI) → 阶段7 UI/反馈。
- **架构红线**：不整体重写 main.js 闭包，用 EventBus+依赖注入(factionContext)；factions/ 模块禁依赖 DOM、必须 sim 无头可跑；不搞五套独立资源(统一金币+状态+系统)；阶段5前不许改数值，每阶段 build+sim/suite 回归。
- **44机制处置**：明细见施工图第四节。关键重构项：普鲁士移动+1→军阵协同；金帐本部骑兵+1→可汗威望动态阈值；蓝帐步兵防+1→伏击阵地；热那亚收入+10%→银行信用借贷；拉古萨商队+5→中立商港；叙利亚异联盟+1→协同射击；巴格达光环→学术指令四态；威尼斯商路乘数削弱改贸易网络；大明溅射并入火力网；马穆鲁克kills×2并入veterancy。7个兵种效果全保留。
- 附属对话 handoff 规范见施工图第五节（路径/必读/硬约束/验收/回传）。

## 踩坑与根因（按时间倒序）
- **2026-09-08 · 阶段4 第三轮：45 个特色兵种行为标签（tags）落地**：按施工图阶段4 + 规格书§14（15 标签池：fortification/raider/scout/trade/escort/veteran/commander/engineer/artillery/ambush/siege/anti_cavalry/amphibious/stealth/support），用 node 脚本按行插入（每行一个兵种对象，锚定英文 type 名 + 行尾 `}`，UTF8 保持，中文零损坏；**踩坑：pikeSquare 同时有 text+bonusVs 需统一行尾插入；annamElephant 是 TYPES 最后一个无尾逗号需 `\}(\s*,\s*)?$` 兼容**）。全部 45 个特色兵种 1-2 个标签。**§15 职责重复检查发现 3 组疑似重复（记待办，阶段5 平衡处理）**：imperialCrossbow vs crossbow（仅 +1 atk+2 cost）、hordeCavalry vs cavalry（同 charge2/atk8 便宜 6）、mingCavalry vs cavalry（同 charge2/atk8 便宜 2）——都是"联盟骑兵/弩手=通用版微调"，缺玩法差异。build 370,080B（+951B tags 数据）；5 harness 全绿；seed777 零变化（tags 无消费方）；suite 3/4（diff-gap 已知旧问题）。tags 当前为数据标注（供阶段6 AI/UI 使用），未接线消费方。
- **2026-09-08 · 阶段4 兵种审查第二轮（打完就跑补全 + 3 描述修正 + 红夷大炮攻城）**：全 src 搜索"打完就跑"发现**金帐联盟机制从未实现**（总览.md 128 行定义"攻击后移动力保留 max(1,floor(maxMove×0.5))"，constants 205 行金帐 style 也写"骑射游击、打完就跑"，但代码只有蓝帐伏击"攻击后满移动力"[nationMechanics 188，注释自认"比联盟打完就跑更彻底"]）。**教训：审查要对照机制总览文档逐条核对实现，文档承诺≠代码实现**。修复：raiding.js onAfterAttack 开头（isGoldenHordeUnit+敌我检查后、掠袭收益前）加 `attacker.move = Math.max(1, Math.floor(attacker.maxMove * 0.5))`——main.js 781 行攻击后 move=0，钩子恢复 50% 实现骑射游击；蓝帐 nations.onAfterAttack 后执行自动覆盖为满（顺序天然正确）。另修：①combat.js SIEGE_DAMAGE 加 `hongyiCannon: 3`（红夷大炮"超远程攻城"=16世纪攻城炮定位，与重型投石车同级；验证 23 vs 20）；②攻城塔描述"占领据点速度×2"→"占领据点后返还全部移动力"（**main.js 883 实际实现是返还移动力，速度×2 无此概念**）；③哈里发学者"周围2格"→"（巴格达3格）"（combat 63 行 scholarRange=baghdad?3:2）；④戚家军"鸳鸯阵"→"鸳鸯阵克制骑兵"（bonusVs cavalry:3 机制）。**已核实不修**：sultanGuard 击杀回血3（main 804）、jinyiwei 免反击（main 809）、龟船反弹30%（main 813）、tradeCaravan/ragusaCaravan 收入（main 878+venice tradeNetwork）、marine"两栖"为 flavor 保留。验证：打完就跑直接断言 PASS（汗国骑2/轻骑3/非金帐0）、build 369,129B、5 harness 全绿、**seed777 零变化**、suite 3/4（diff-gap 已知旧问题）。阶段4 剩余：tags 审查 + 最后一批兵种核对。
- **2026-09-08 · 阶段4 兵种审查第一轮（7 项处置，A 类补机制/B 类核实后按最初要求修）**：审查基准=60 兵种描述 text vs TYPES 字段 vs combat/movement/main 实现 + 规格书（近卫军=守点、投石车=攻城 是规格书明确定位）。**修复**：①combat.js 新增 `SIEGE_DAMAGE={catapult:2,heavyCatapult:3,siegeCrossbow:2,nomadChariot:2,nomadCannon:2}`，computeDamage 在目标格有据点时 `nationAtk += SIEGE_DAMAGE[attacker.type]||0`（与普鲁士+3 不同来源可叠加）；②通用近卫军守点防御+1（imperialGuard 仍+3）；③巴伐利亚山地弩手驻扎山地（站在 hill）射程+1（effectiveRange）；④movement.js 阿拉伯弓手**兵种级**沙漠（desert）移动不消耗（其他都是 nation 级判断——arabArcher 是任何马穆鲁克国家可造的兵种特性，故用 type 判断）；⑤constants.js 三处描述修正：masterEngineer/worksEngineer 删掉无实现的"可建高级营地/速度+50%"并把折扣注明为"威尼斯本部-20%/大明本部-10%"（实际是国家机制非兵种）、shenjiBattalion 描述精确化"溅射50%（大明本部60%）"。**核实不修**：jihadist"对异联盟+2"=马穆鲁克联盟圣战机制（combat 54 行）属实；溅射 60%→80% 是规格书"错误方式"举例非设定。**验证坑（3 个全是测试脚本问题，非代码问题）**：①defender 血太少被 clamp 吃掉伤害差（换 hp30 高血单位）；②`movementCost(game,u,x,y)` 读 `terrain[y][x]`——**传参行列写反**导致测到 plain cost1，arabArcher 假 PASS；③"有据点 vs 无据点"对比混入据点防御加成，正确对照=同据点目标下 crossbow(无攻城) vs catapult(+2)（SIEGE 进 base 会被 hpFactor 放大，伤害差≈(atk差+2)*1.2）。**验证结果**：直接断言全 PASS、build 368,991B、5 harness 全绿、**seed777 完全零变化**（默认局 hre+austria 不用攻城/守点/山地/沙漠兵种）、suite 3/4（diff-gap 已知旧问题）。阶段4 剩余：剩余兵种（约 20 个）逐一核对 + tags 审查。
- **2026-09-08 · main.js 拆分窗口第四刀（收官）：turn.js 回合推进**：turn.js 追加 `advanceTurn` + `beginTurn`（模块内互调：advanceTurn→resolveStalemate/beginTurn；beginTurn→advanceTurn/checkEnd 全部本模块）。turn.js 补 import（eventBus/statusSystem/MAX_TURNS——第一刀无 import 全注入）。main.js 用 `turnFlowDeps = { ...turnDeps, ownerExists, decayFrontMemory, decayTemporarySites, healOwner, grantIncome, aiRepair, unit, getUnit, effectiveMove, refresh, aiTurn, fastSim: () => fastSim }`——**关键：turnFlowDeps 必须定义在 turnDeps 之后**（spread 引用，TDZ），所以放在胜负判定子组处，beginTurn/advanceTurn 薄包装放原位置（函数体内运行时才求值，无 TDZ 问题）；`fastSim` 用 `() => fastSim` 动态读取（不能快照值）。AI 调度（setTimeout aiTurn）留在 turn.beginTurn 内，deps.aiTurn 注入。build 368,390B；seed777 全量一致（121/1941/224/89/8/3/2/71/6/0/301）；5 harness 全绿；suite 待跑。**main.js 拆分窗口四刀全部完成（turn 胜负判定 6 + economy 9 + factory 19 + turn 回合推进 2 = 36 函数抽离）**。
- **2026-09-08 · main.js 拆分窗口第三刀：factory.js（生产构造）**：新建 `src/core/factory.js` 抽取 19 函数——unit / createCargoPayload / createLoadedTransport / site / createCamp / unitBuildCost / sellRefund / cargoOptionTypes / normalizeCargoTypes / rankFromKills / effectiveMove / healMultiplier / grantKills / transportCost / factionAdjustedCost / cargoLabel / describeCargo / buildableTypes / buildAtSite。**四合一 factionAdjustedCost 原样搬移（威尼斯 1.5×/拉古萨 0.95×/威尼斯本部造船 0.8×/大明本部造船建营 0.9×）——用户点名"老规矩"不能误伤**，deps 注入 ownerFaction/ownerNation；buildAtSite 依赖的 buildBudgetLeft/recordBuild 从 main.js 的 economy 薄包装注入（不跨模块互引，保持 deps 统一）。sellUnit/forceCrowding/capacityPressure 留 main（有 UI 状态）。build 367,776B；seed777 全量一致；5 harness 全绿。
- **2026-09-08 · main.js 拆分窗口第二刀：economy.js（收入/据点经济）**：新建 `src/core/economy.js` 抽取 9 函数——supportSites / healOwner / grantIncome / siteUpgradeCost / buildBudgetLeft / recordBuild / upgradeSite / fullHealSite / aiRepair（healOwner 调 supportSites 模块内部互调，无需注入）。**踩坑：main.js 的 `dist/siteMeta/typeMeta` 全部来自 `./core/utils.js` 而非 constants.js**（constants.js 只有大写 SITE_META/TYPES）；第一版 import 从 constants 拉 dist/typeMeta 导致 esbuild ERROR，改为 `import { siteMeta, typeMeta, dist } from './utils.js'` 后通过——**core 模块 import 时先查 main.js 顶部 import 块来源，别凭模块名猜**。build 364,301B；seed777 全量一致（121/1941/224/89/8/3/2/71/6/0/301）；5 harness 全绿；suite 待跑。剩余刀：factory.js（unit/site/createCamp/createLoadedTransport/buildAtSite/factionAdjustedCost/transportCost/describeCargo 等生产构造）、回合推进（beginTurn/advanceTurn，依赖 AI 回调+DOM，最后做）。
- **2026-09-08 · main.js 拆分窗口第一刀：turn.js（胜负判定子组）**：新建 `src/core/turn.js` 抽取 6 个函数——teamStandings / resolveStalemate / landUnitCanReachForeignCity / teamCanContestLand / dominantCityTeam / checkEnd。抽取方式=**纯逻辑迁移 + deps 注入**（本模块零 DOM；闭包辅助函数 teamOf/areAllies/teamName/typeMeta/isTransportUnit/cellKey/getSite/adjacent8/isLandTile/finish 经 `turnDeps` 传入，main.js 传原函数保证逐字节一致）；main.js 原函数体替换为薄包装（`function checkEnd() { return turn.checkEnd(game, turnDeps); }`），调用点零改动。**finish/endGameNeutral/sideLabel 留在 main.js（有 DOM 依赖）**，advanceTurn/beginTurn 是下一刀。验证：build 363,003B；**seed777 逐项与基线完全一致**（121/1941/224/89/8/3/2/71/6/0/301）；5 harness 全绿；suite 待跑。经验：先抽"无 DOM 副作用"的纯判定函数最安全；参数顺序 game→deps→业务参数统一。
- **2026-09-08 · 阶段3 完成：15 国机制 6 项全部落地 + 临时桥（主对话执行）**：①临时桥（裁决③）——core 通用移动成本钩子 `movement.js` movementCost 末尾叠加 `facilitySystem.getMoveCostModAt(x,y)`（`Math.max(1, cost)`），core/facility.js 新增 `getMoveCostModAt`（只认 `data.moveCostMod` 数字字段，**core 不识别设施类型**，防联盟 if 泄漏）；大明 engineering.js 栈桥 causeway 整体替换为 bridge（cost12/hp10/duration3/moveCostMod-1，desc"桥格移动成本 -1，敌我均可利用"）；resolveDeploy 用 `data:{...}` 包裹（**首个黑箱 bug：facility.createFacility 只取 opts.data，顶层字段被丢弃**→切段定位修复）；harness M10 重写 4 断言（83 PASS）。②金帐三国机制 `goldenHorde/nationMechanics.js`（写入+接入 goldenHordeRules.js，新增 onBeforeAttack 分发）：可汗威望（击杀+1，阈值5=骑兵首攻+1/10=营地成本×0.8（口径：camp 立即生产无时间概念，用折扣近似，阶段5 可调）/15=khanGuard+2）、白帐绿洲（白帐控制 desert/sand 据点 2 格内：回血+2/对骑兵+2/击杀掠袭+2——**掠袭+2 必须 defenderDead 才给，否则刷钱漏洞**）、蓝帐伏击（snow/forest 首攻+2，afterAttack 后 move=maxMove 可撤离）。③HRE 国家机制 `hre/nationMechanics.js`（已接入）：普鲁士军阵协同（相邻友军每回合首次移动预支+1——**不能 Math.min(maxMove) 钳制，否则满移动力出发时预支被吃掉**（harness 抓到 got3 want4）；巴伐利亚山地（丘陵受击-1）。④旧固定加成移除（防叠加）：combat.js 78/81/83（金帐本部骑兵+1/白帐对骑兵+2/蓝帐步兵防+1）+ main.js effectiveMove prussiaBonus（753）；**combat.js 82 prussia 据点+3 保留**。⑤nomadCamp 联动：本部营地 duration+2（harness 5→7 更新）、威望10 生产×0.8、绿洲内营地+1。⑥harness 回归新增 N1-N3 测试（hre 10 条/goldenHorde 13 条；需要 `setPrestigeForTests` 注入导出）。**验证**：5 harness 全绿（hre/goldenHorde/venice ALL PASS + mamluk 93 + ming 83）；seed777 默认局逐项与基线一致（combat 移除行默认局不触发→零变化预期达成）；注入局：金帐本部 sells 301→72、蓝帐 cityCaptures 8→14 + retreats 89→13 + campsBuilt 3、白帐 sells 78、大明与上次完全一致；suite 3/4（diff-gap 50% 为已知旧问题）。**产物**：js/game.js 362,042B（已重新 build）。
- **2026-09-08 · 队伍系统 UI 修复（15 队+历史队名，用户拍板）**：TEAMS ['A'..'E']→['A'..'O']（15 队）；TEAM_NAMES=A汉萨同盟/B拜占庭帝国/C条顿骑士团国/D莫斯科大公国/E奥斯曼帝国/F帖木儿帝国/G蒙古帝国/H波兰-立陶宛联邦/I匈牙利王国/J萨法维波斯/K德里苏丹国/L高棉帝国/M高丽王朝/N马里帝国/O埃塞俄比亚帝国（全历史真实名，避开 5 联盟名）；UI"组别"列→"队伍"、h2"玩家与联盟"→"玩家与队伍"；下拉/侧栏/胜负播报全用队名（`teamName(t)` helper，未知字母回退 `t组`）；**236 行观战胜者正则同步改**：`^(\S+)\s*组`→`^(.*?)\s*组`+TEAM_NAMES 反查（旧档字母/新文本队名都兼容）。渲染逻辑用 TEAMS.map+TEAM_NAMES 自动适配 15 队（main.js 零额外改动）。sim 的 teams 是独立硬编码（run.js ['B','C','A']、harness.js ['B','C','A','D','E']），不依赖 TEAMS，seed777 逐项与基线一致。队=同盟分组，独立于联盟/国家（玩家+AI 自由分配）。**4 项裁决用户全部接受**：①雇佣兵保留双入口 UI 合并；②学者叠加接受；③临时桥钩子阶段3 补；④stealth 阶段6。
- **2026-09-08 · 阶段3 缺口核对（27 国机制处置表 vs 已实现）**：威尼斯三国（垄断/银行/中立港）✅ 马穆鲁克三国（尼罗河/火线/学术）✅ 大明三国（锦衣卫/水寨/伏击）✅ 奥地利 charge+1 保留 ✅。**未实现 6 项**：普鲁士军阵协同（重构替代恒定+1）、巴伐利亚山地防线（整合丘陵）、金帐本部可汗威望（重构，5/10/15 阈值）、金帐本部营地+2 回合（整合）、白帐绿洲网络（整合对骑兵+2）、蓝帐伏击阵地（重构雪地森林边缘）。联盟级商路经济削弱已由威尼斯对话实现（收入转贸易路线）。**4 待裁决项建议**：①雇佣兵老规矩 vs 市场双入口→保留双入口、UI 合并展示（服务不同决策）；②学者四选一 vs 光环+1 叠加→**接受叠加**（combat.js 冻结，强度靠阶段5 数值调）；③临时桥移动成本钩子→**阶段3 补 override 点**（大明栈桥工事已兜底）；④stealth 接 AI 索敌→**阶段6 与 AI 改良一起**（机制全定再动 AI）。
- **2026-09-08 · 大明集成验收通过，五大联盟 Layer4 全部完成（里程碑）**：接线后 seed777 默认局逐项与基线一致（五联盟注册零扰动）；大明注入局差异符合画像（cityCaptures 8→10、campsBuilt 0→3、sells 301→92——远程火力+工程攻城）；suite 3/4（已知旧问题）。**产物 350,854B（+21.5KB 大明代码），五个 harness 全绿（ming82/hre79/goldenHorde48/venice100/mamluk93）**。两个风险点按推荐降级：临时桥→栈桥工事（桥格+1防御，无移动成本钩子，阶段3 若补 override 钩子可升级）、锦衣卫 stealth→'hidden' 状态标记（AI 索敌影响阶段6 处理）。工程设施决策仅 player（AI 不建），fireZone/crossfire/水寨/伏击自动部分 AI 生效。**下一阶段：阶段3 十五国机制重做 + 阶段5 平衡（含待裁决：雇佣兵老规矩/学者叠加/临时桥/stealth）+ main.js 拆分窗口（turn→economy→factory，每抽一个跑 seed777）**。
- **2026-09-08 · 大明 handoff-E 要点（五大联盟最后一个）**：火力网 fireZone（大明远程攻击后目标格生成 facility，duration 1，敌人**移动进入**时受伤[beforeMove 检测 to 格]，交叉火力 crossfire=≥2 个火力区覆盖 → beforeAttack 增伤）+ 工程设施 5 种（炮台[防御buff+turnStart结算，勿做新战斗循环]/瞭望塔[火力校正]/补给站[范围回血,埃及先例]/壕沟[减冲锋,beforeAttack]/临时桥[**移动成本钩子缺口:ctx.movementCost 是纯函数+core/movement.js 禁改→推荐降级为桥格buff 或报告主对话申请 override 钩子,登记排期**]）。三国：锦衣卫 stealth（**AI 索敌风险→降级标记,阶段6处理**）/朝鲜水寨（港口海岸减伤）/安南丛林伏击（森林首轮攻击加成）。AI 不建工程设施（决策仅 player）。文件 `.vibecoding/v0.2-handoff-E-ming.md`。**main.js 拆分窗口：大明验收后、阶段3 国家机制重做时**（turn.js→economy.js→factory.js 渐进抽，每抽一个跑 seed777）。
- **2026-09-08 · 马穆鲁克集成验收通过，两个口径偏差待阶段5裁决**：① 学者四选一"取代攻防+1"→ 实际为**叠加**（combat.js 冻结不可改，既有光环+1/+1 仍在 + 战术额外加成），登记待阶段5 由主对话改 combat.js 实现"取代"或接受叠加；② 机动战术受 reachable 先于 beforeMove emit 计算的物理限制，按"每回合首次移动消耗-1"（预支+1移动力）实现。另：eliteLost 只覆盖战斗死亡（afterAttack 双端）；AI 精锐 V3 不晋升（停在 V2，防 sim 自动选第一项悄悄改 AI 数值）。接线后 seed777 默认局逐项与基线一致；马穆鲁克注入局差异符合精锐成长画像（retreats 89→1、sells 301→73、stalls 1833）。**注意：Grep 在 Windows 下中文 pattern 匹配不可靠，验证 MEMORY 内容用 Read 全文或英文/ASCII 关键词。**
- **2026-09-08 · 马穆鲁克 harness 修复（4 条教训，D 对话交付）**：① 喂 XP 判定关键战斗用 `typeMeta(unit.type).level`——**在 unit 对象上显式覆盖 `level` 字段无效**（typeMeta 读 TYPES 常量），killOnce 目标必须用 level≥3 兵种（heavyInfantry=2/militia=1 都不触发 +5 关键战斗加成）；② `freshGame` 后先 `setMoraleForTests` 再触发钩子会被钩子内 `syncGameRef` 清掉——跨局重置应先显式调 `vet.syncGameRef(ctx)` 再设测试状态；③ decision 记录结构是 `{id, context, ...}`，**options 在 `context.options`**（取选项/断言顺序要解包 context）；④ 决策 fallback=第一项（无 UI 自动选）是"无头 sim 零变化"验收线的保证，第一项必须设默认/不行动。交付文件 `.vibecoding/v0.2-delivery-D-mamluk.md`。
- **2026-09-07 · 马穆鲁克 handoff-D 要点**：精锐成长（veterancy：经验=伤害/击杀/占领→V1攻+1自动/V2防+1自动/**V3三选一决策**冲锋/回血/移动→Elite称号）+ 士气（精锐击杀+1死亡-3；高=经验快+骑兵首攻增强，低=新生兵攻击降+**精锐恢复降低[GH-06缺口,推荐降级]**）+ eliteLost（V3+精锐死亡=金币+士气-3+经验清零）。三国：埃及尼罗河补给（河流/城市旁回血+经验，turnStart 直改 unit.hp，金帐先例）/叙利亚长弓火线（协同射击）/巴格达学术指令（**学者四选一**：守势/进攻/机动/整军）。**关键决策：AI 精锐 V3 不自动晋升（停在 V2）**——不能给 AI 发晋升决策（sim 自动选第一项=悄悄改 AI 数值）。经验/死亡检测用 afterAttack.defenderDead，不用 unitKilled（GH-09）。士气改伤害挂 beforeAttack（注册顺序 hre→gh→venice→mamluk 叠加）。文件 `.vibecoding/v0.2-handoff-D-mamluk.md`。
- **2026-09-07 · 威尼斯集成：契约 §6 钩子签名表整体与实现不符**：registry 实际分发 `system[hook](ctx, payload)`，契约 v1.2 表却写 `(ctx, owner, initial)`/`(ctx, owner, amountRef)` 等多参数写法——HRE/GH 一直按 `(ctx, payload)` 实现没冲突，威尼斯 `onIncomeCalculated` 明确要求"直接改 amount"才暴露。教训：**契约签名必须以 factionRegistry 分发代码为准**（威尼斯对话已把这条写进它的经验教训）。已修契约 §6 统一为 `(ctx, payload)` + v1.3 变更记录。
- **2026-09-07 · incomeCalculated 引用语义验证**：main.js grantIncome 用 `incomePayload = {owner, amount}` 对象 emit 后读 `incomePayload.amount`（引用传递）——威尼斯 `payload.amount += bonus` 真实生效。判定：**凡"钩子改 payload 字段"必须核实 main.js 埋点是否引用语义**，不能只看 harness（harness 自己构造 payload 会假绿）。
- **2026-09-07 · 威尼斯接线后默认局零变化、注入局差异符合画像**：transportLaunches 6→29（海军经济+港口半价产船）、engineerLandings 71→1、sells 301→125、retreats 89→0；AI 不建路线/不借贷（决策仅 player）→ 被动机制（垄断产船/1.25 收入）对 AI 生效是设计意图。
- **2026-09-07 · 威尼斯 handoff-C 要点**：贸易网络 Layer4 = tradeRoute facility（**关系型实体**，data 承载 startNode/endNode/path/income；与 HRE 区域性工事、金帐单体营地不同）；收益 +2/回合、每额外 2 格 +1、单条上限 +5；敌军进入路线 → tradeDisrupted（**优先挂 incomeCalculated 检测，不新增埋点**）；三国机制 = 威尼斯海上垄断（2/3 港）/ 热那亚银行信用借贷（+15、3×-6、不能无限用）/ 拉古萨中立商港（敌方港口→交易港）；雇佣兵升级涉 main.js 旧机制 → 附属对话不得自行改，先报告。文件 `.vibecoding/v0.2-handoff-C-venice.md`。
- **2026-09-07 · 金帐接线后默认局零变化、金帐局有差异**：注册 `goldenHorde` 后，默认 hre 局（isGoldenHordeOwner=false）所有钩子空转 → seed777 逐项与基线一致；金帐镜像局统计变化（engineerLandings 5→17、transportLaunches 10→23、sells 65→50）证明掠袭经济真实生效——**接线验证的判定标准：默认局必须零变化，注入局必须有可解释差异**。
- **2026-09-07 · 金帐模块级跨局状态靠 syncGameRef 自动清**：`state.slowUsedThisTurn` 是模块级 Set，不是 game 状态；raiding.onTurnStart 每回合对比 `ctx.game` 引用，换局自动 reset——无需在 newGame 额外接线（与 HRE 用 facility 存储不同）。
- **2026-09-07 · tickStatuses 只传 owner 会误删其他方状态**：status.js 的 `tickStatuses(aliveUnitIds)` 对"不在列表者"执行**删除**（status.js:39-41）。若只传当前 owner 存活单位，其他 owner 存活单位的状态（如敌方单位的 raided）会在该方回合被误删，金帐"下轮攻击额外收益"永远失效。**必须传全量**：`tickStatuses(game.units.map(u => u.id))`（main.js beginTurn 开头、if(!initial) 前、早于 turnStart emit）。
- **2026-09-07 · raided 写 turns=3 而非 2**：在"每 beginTurn 全量 tick 一次"语义下，turns=2 会在金帐下轮攻击前被清 0（额外收益失效）；turns=3 才能保证：敌方回合 tick→2（移动-1 覆盖敌方恰好 1 个完整回合）→ 金帐下轮 tick→1（攻击时额外收益生效）→ 再下轮归零。玩家可见"持续1回合"= 敌方 1 个完整回合。
- **2026-09-07 · ctx.createUnit 必须经 main.js deps 注入**：单位工厂 `unit()`/`randomId()` 在 main.js 闭包内，factionContext.js 无法 import——由 initFactionSystems deps 注入 `createUnit(type,owner,x,y)`（复用 unit() 工厂 + push + incrementStat('produced')），ctx 只转发。unitCreated 未埋（main.js 全部生产路径统一排期补，不在 createUnit 半埋）。
- **2026-09-07 · sim 里 AI 联盟默认全是 hre+austria**：`newGame` 里 `$('ai${i}Faction')?.value || 'hre'`，而 sim/run.js 的 config **没有注入 `ai${i}Faction/Nation`**，elFor shim 返回 `''` → 回退到 `'hre'/'austria'`。含义：所有无头 sim 场景的 AI 实际全是神罗（v0.1.2 起即如此），后续联盟（金帐等）接入后 sim 仍只测神罗路径。**已解决（2026-09-07）：sim/run.js 支持 `ai${i}Faction/Nation` 注入，仅显式传参时生效**。
- **2026-09-07 · HRE 接线后 seed777 零变化的原因**：sim 无 `player` owner → 神罗建造决策不发起（`owner==='player'` 分支）→ 无工事 → 木栅税/阵线/工事效果全不触发；兵种联动在本次对局未改变终局统计。这是"预期内零变化"，不是接线失败（接线已由 __hreDebug 挂载冒烟证明）。
- **2026-09-07 · suite 耗时约 120~164s**：HRE beforeAttack/afterAttack 钩子每次攻击全单位扫描导致性能回归（Phase1 约 74~84s）。正确性无影响，待优化（空间索引）。
- **2026-09-06 · PowerShell 5.1 不支持 &&**：给用户的终端提交命令用了 `git add -A && git commit -m "..." && git push`，用户的 PowerShell 5.1 报"不支持 &&"。**教训：给用户的终端命令一律用 `;` 分隔，不用 `&&`**（用户环境是 Windows PowerShell 5.1，不是 PowerShell 7+）。
- **2026-09-05 · 抽取边界事故**：重构删除段用字符串锚点（`siteBonus` 定义 → `attack` 定义）定位，未核实区间内是否含其他函数，误删了有副作用的 `removeUnit`，导致 sim 报 `removeUnit is not defined`。教训：**切段删除前先列出区间内全部函数清单，副作用函数（改状态/日志/统计）一律留在 main.js**；错误信息直接指认根因（黑箱协议第 4 步）。
- **2026-09-05 · 编码事故**：用 PowerShell `Get-Content -Raw` + `Set-Content -Encoding utf8` 改 main.js，导致全部中文变乱码（PS 5.1 默认按 ANSI/GBK 读取 UTF-8 文件）。根因链：PS 读取编码错误 → 乱码 → esbuild 报 Unterminated regular expression。**教训：改含中文的 JS 文件禁止用 PowerShell 文本管道，一律用 Node 脚本或 Read/Edit 工具；git restore 可回滚。**
- 2026-09-05 · 构建产物"过期"实为注释级差异：main.js 最后 6 分钟改动未改变 esbuild 输出内容（legalComments:none），但时间戳失联导致无法证明同步，重建后消除不确定性。

## 用户偏好与约定
- 用户偏好 VibeCoding 工作方式：每轮收尾列改动+影响+教训；分支任务拆到附属对话省 tokens——**AI 要在合适的时机主动提醒并指导用户去开附属对话**。
- 用户要求方案简单可操作；技术问题要求深度解析、拒绝黑盒结论。

## 阶段5（2026-09-08）记录
- **2026-09-08 · 统计基础设施落地**：为规格书 §29 的 11 项指标新增 stats 桶——battles/moves/incomeBySource/producedByType/killedByType/lostByType（SPECIAL_TYPES 从 TYPES.faction 推导，只记 45 特色兵种）。addGold 加 source 参数（faction ctx 传 'raid'/'genoaLoan' 自动入 incomeBySource）。grantIncome 薄包装统计 base。debugRunResult 加 balance 终局快照（gold/unitsAlive/level3Units/cityCount/per-type）。sim/run.js 聚合 balance（嵌套对象递归平均）。验证：seed777 旧指标逐项零变化 + suite 3/4 + 5 harness 全绿。
- **2026-09-08 · economy.grantIncome 无 return 的坑**：抽取版 grantIncome 从不返回收入额（只有 log+加钱），薄包装 const income = economy.grantIncome(...) 得到 undefined → 统计被 income > 0 拦截 → incomeBySource.base 全 0。修复：economy.js 补 return incomePayload.amount（原调用方不依赖返回值，零行为变化）。**教训：core 抽取函数改前先确认返回值契约，不要假设有 return。**
- **2026-09-08 · producedByType 空≠埋点坏**：默认局 strait 图 AI 走海战 AI（navalProduce），造 guard/spearman/crossbow/archer 全是**通用兵种**（无 faction 字段）→ SPECIAL_TYPES.has() 全 false → per-type 桶空。注入金帐/大明局后 producedByType/killedByType/lostByType 正常出数（khanGuard 损失 61、hongyiCannon 击杀 19 等）。**教训：per-type 统计只在特色局有数据，默认局空是预期。**
- **2026-09-08 · strait 图 C 侧系统性优势（约 8:1）**：5 联盟镜像局（rounds=3）胜场分布全是 C(ai1) 8:1——**位置偏差**（suite 已知 mirror-strait B 胜率 38%）。**阶段5 铁律：平衡对比必须 ai0/ai1 对调取平均，单侧数据全部无效**；跨联盟单局（horde vs ming 等）被污染不可作平衡结论。
- **2026-09-08 · 阶段5 首版数据画像**：金帐暴兵流（produced 286:248 镜像、khanGuard 大量损失）；威尼斯回合收入 base 最高（19984，贸易加成生效）；各联盟 level3 都有积累（veterancy 生效）；金帐 raid 收入 909~1161（掠袭机制活跃）。候选调整：金帐产能、大明 level3 速度、3 组职责重复兵种、雇佣兵老规矩、可汗威望 10 档口径。
- **2026-09-08 · diff-gap 50% 归阶段6**：冷酷 AI 胜率不足是 AI 难度问题（机制全定才练 AI），登记为阶段6 前置验收项，阶段5 数值不动。
